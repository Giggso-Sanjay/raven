

claude-plugins-official/
.DS_Store
guard/.git
raven-plugin.zip
!plugin/raven-plugin-*.zip
.claude/.raven/
codex/.raven/

# Do-not-share working documents
*.dns
.model.env

# Internal-only working docs — NEVER share to repo
local/
*.md.env
*.local.md
docs/APPLY-PROMPT-vault-graph-memory.md

# Feature docs + upgrade journey — kept LOCAL by owner decision (2026-08-13).
# Untracked, not deleted: the files stay on disk and are still required by
# open_raven/CLAUDE.md Rule A. Consequence accepted — a fresh clone cannot trace
# these features from trigger to effect. Re-track with `git add -f <path>`.
docs/features/
docs/JOURNEY-4.2-to-5.0.md

# Raven secrets — NEVER commit
.raven/manifest.secrets.json
**/manifest.secrets.json
*.secrets.json

# Raven runtime audit logs — local-only, not source
.raven/audit/

# Python bytecode
__pycache__/
*.pyc
.env
.raven/.cache/
.raven/state/
.raven/.model-session.json
.raven/.token-meter-checkpoint.json
.raven/xray.json
.raven/xray-stamp.json
.raven/.router-state.json
.raven/.push-mode
.raven/.push-approved
.raven/.push-notice-shown
.raven/cost-log.jsonl
scripts/.raven/

# Raven cross-team handoff docs — local artifact, share manually, don't commit
.raven/handoff/
raven-dash-kg.md
.raven/.cost-verify.json
**/.raven/state/
**/.raven/.model-session.json
