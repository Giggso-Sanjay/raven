---
description: Raven router and cost must run every user turn
alwaysApply: true
---

First load: `ide-boot.py` then Read `memory=` card if load=1. Every turn before any other tool: `bash scripts/raven-python.sh scripts/routing/model-router.py --prompt "<user message>"`. Print 🔀 then 💰 then educate= expected= then session= then Intent:. `--session-start` on first load. git log/status is not an exemption. educate=guided: briefing then STOP until go ahead.
