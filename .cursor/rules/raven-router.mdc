---
description: Raven router and cost must run every user turn
alwaysApply: true
---

Before any git, read, or answer: run `python3 scripts/routing/model-router.py --prompt "<user message>"`. Print 🔀 then 💰 then educate= expected= then session= then Intent:. First load also run `--session-start`. git log/status is not an exemption. Skipping leaves dashboard Logs empty for this IDE. educate=guided: briefing then STOP until go ahead — no writes before that.
